package com.izaanschool.ifelse;// Expense - A class representing an expense with properties like date, amount, and category.

      // Declare Main Method
    
    
        // Declaring variables and initializing it with a value
        

        // Declare if Condition: using int  --- if amount is 450 then print message "Isn't it too expensive"
        
